# This has some headers
# These are comments that happen before any sections are defined

        # These are stored separately and restored as-is


    [DEFAULT]
    foo=bar
    # Comment here
    bar = foo
